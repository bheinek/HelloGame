import { Routes } from "react-router-dom";

export const PongRoutes = () => {
  return (
    <Routes>
      {/* <Route element={<TicTacToe />} path="/" />
      <Route element={<CompletedGame />} path="/completed-game" /> */}
    </Routes>
  );
};
